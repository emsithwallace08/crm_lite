# My Status

Fill this in as you go. If you're blocked, say what's blocking you — the
instructor will check this file when walking the room.

- [ ] Toolchain installed (Node, Git, VS Code, Claude Code)
- [ ] Repo copied locally
- [ ] `pnpm install` succeeded
- [ ] `.env` created from `.env.example` with a working `DATABASE_URL`
- [ ] `pnpm db:push` succeeded
- [ ] `pnpm db:seed` succeeded
- [ ] `pnpm dev` running — contacts and deals visible at http://localhost:3000

**Status:** 🟢 / 🟡 / 🔴  (delete the two that don't apply)

**Blocker (if any):**

**Ticket I'm working on (from ISSUES.md):**
